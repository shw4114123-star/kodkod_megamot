services:
  mysql:
    image: mysql:8
    container_name: iron-dome-db
    restart: always
    ports:
      - "3306:3306"
    environment:
      MYSQL_ALLOW_EMPTY_PASSWORD: true
      MYSQL_DATABASE: iron_dome_db
    volumes:
    - mysql-data:/var/lib/mysql
volumes:
  mysql-data: