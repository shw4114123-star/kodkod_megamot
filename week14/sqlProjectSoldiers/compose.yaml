services:
  mysql:
    image: mysql:8.0
    container_name: db_project
    ports:
      - "3306:3306"
    environment:
      MYSQL_DATABASE: "soldier_db"
      MYSQL_ALLOW_EMPTY_PASSWORD: true
    volumes: 
      - mysql:/data/db
volumes:
  mysql: